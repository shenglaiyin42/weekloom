# 匿名示例数据

本目录只保存用于开发、测试和文档演示的匿名示例数据。

示例按实体类型分目录：

```text
examples/data/
├── projects/
├── actions/
├── weeks/
├── inbox/
└── reviews/
```

可以使用 `python scripts/validate_data.py --data-dir examples/data` 校验这些示例。

不得复制真实工作项目、个人计划、生活事项、复盘内容、身份信息或访问凭据到本目录。
